# NEXUS v1.0 – Python Dependencies
# Install: pip install -r requirements.txt
# Python 3.10+ required
#
# All optional packages are clearly marked.
# NEXUS runs with only the "Core" packages -- optional ones unlock extra features.

# -- Core (always required) -------------------------------------------
requests>=2.31.0
feedparser>=6.0.0
beautifulsoup4>=4.12.0
colorama>=0.4.6

# -- Search -----------------------------------------------------------
duckduckgo-search>=6.0.0

# -- Data / ML (Prediction, Anomaly Detection) ------------------------
scikit-learn>=1.3.0
numpy>=1.24.0

# -- Network Graph Analysis (nexus_graph, nexus_netgraph) -------------
networkx>=3.2.0

# -- PDF Export (nexus_report) ----------------------------------------
reportlab>=4.0.0

# -- AIS WebSocket (nexus_ais) ----------------------------------------
websocket-client>=1.6.0
websockets>=12.0

# -- Satellite Timing (nexus_satellite_timing) ------------------------
ephem>=4.1.5

# -- Image Analysis (nexus_imgcheck, nexus_geolocate) -----------------
Pillow>=10.0.0

# -- OPTIONAL: Text-to-Speech -----------------------------------------
# edge-tts: high-quality neural TTS (requires internet)
#   pip install edge-tts
# pyttsx3: offline TTS fallback
#   pip install pyttsx3>=2.90

# -- OPTIONAL: Speech Recognition (STT) ------------------------------
# Google STT (free, requires internet):
#   pip install SpeechRecognition>=3.10.0
#   pip install sounddevice>=0.4.6   (or: pip install pyaudio>=0.2.13)
#
# Whisper offline STT (best quality, no internet):
#   pip install faster-whisper>=1.0.0
#
# Vosk offline STT (fast, small footprint):
#   pip install vosk>=0.3.45
#   Download model: https://alphacephei.com/vosk/models -> vosk-model-small-de-0.15

# -- OPTIONAL: OCR ----------------------------------------------------
# pip install pytesseract>=0.3.10
# Also requires: Tesseract binary from https://github.com/tesseract-ocr/tesseract

# -- OPTIONAL: NLP / Named Entity Recognition -------------------------
# pip install spacy>=3.7.0
# After install: python -m spacy download de_core_news_sm

# -- OPTIONAL: Computer Vision ----------------------------------------
# pip install opencv-python>=4.8.0

# -- OPTIONAL: Audio playback -----------------------------------------
# pip install pygame>=2.5.0

# -- OPTIONAL: Windows desktop notifications (Windows only) -----------
# pip install win10toast>=0.9

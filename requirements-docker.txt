# NEXUS – Docker-Abhängigkeiten (Web-Server-Stack)
# Kein Audio, kein TTS, kein Windows-spezifisches

# HTTP + APIs
requests>=2.31.0
urllib3>=2.0.0

# Web-Scraping
beautifulsoup4>=4.12.0
feedparser>=6.0.10
lxml>=4.9.0

# Suche
duckduckgo-search>=5.0.0

# WebSocket (AIS-Stream)
websockets>=12.0
websocket-client>=1.7.0

# Daten / Wissenschaft
numpy>=1.24.0

# PDF-Export
reportlab>=4.0.0

# OCR (optional, wird graceful übersprungen wenn fehlt)
# pytesseract>=0.3.10
# Pillow>=10.0.0

# NLP (optional)
# spacy>=3.7.0

# Web-Server für nexus_live_server.py
# (benutzt nur stdlib http.server + threading)

# Datenbank (stdlib sqlite3 reicht)

# OSINT-spezifisch
# Keine weiteren externen Libs nötig
